import { makeStyles } from '@mui/styles';

const useStyles = makeStyles({
  container: {
    paddingTop: 30,
    paddingBottom: 30,
    paddingLeft: 30
  },

  link: {
    textDecoration: 'none',
    color: 'black',
    fontWeight: 500,
    fontSize: 16
  }
})

export default useStyles
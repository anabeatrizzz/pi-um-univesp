import { makeStyles } from '@mui/styles';

const useStyles = makeStyles({
  firstGrid: {
    paddingLeft: 20
  },

  body1: {
    marginBottom: 15
  },

  container: {
    paddingTop: 30,
    paddingBottom: 30,
    paddingLeft: 5
  },

  link: {
    textDecoration: 'none',
    color: 'black'
  }
})

export default useStyles